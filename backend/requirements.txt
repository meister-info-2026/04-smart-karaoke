fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
pymysql>=1.1.0
python-dotenv>=1.0.1
httpx>=0.27.0
pydantic>=2.6.0

# Supabase(PostgreSQL) 배포용 드라이버.
# DATABASE_URL이 설정된 경우에만 사용되며, 로컬 MariaDB 개발에는 필요 없다.
psycopg[binary]>=3.1
