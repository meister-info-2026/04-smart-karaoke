# ==============================================================================
# [Vision 초경량 환경 가이드 (Pure ONNX & OpenCV)]
# PyTorch/Ultralytics를 완전히 제거하고 ONNX Runtime 및 OpenCV로 구동됩니다.
# - 수백 MB의 PyTorch 다운로드 없이 5~10초 만에 초고속 설치 완료
# - Windows Python 3.14/3.13 혼재 시 DLL 에러([WinError 1114]) 및 C++ 빌드 에러 원천 차단
#
# 가상환경 생성 및 설치:
#   py -3.12 -m venv venv
#   .\venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
# ==============================================================================

# 초경량 고성능 추론 엔진 (PyTorch 불필요, CPU 가속 탑재)
onnxruntime>=1.18.0
opencv-python>=4.9.0
numpy<2

# 웹캠 UI 한글 깨짐 방지 폰트 렌더링
pillow>=10.0.0

# 고성능 벡터 거리 계산 및 수치 연산 (얼굴인식 임베딩 비교용)
scipy>=1.12.0

# 백엔드 REST API 통신 및 환경변수
requests>=2.28.0
python-dotenv>=1.0.0


